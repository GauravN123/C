Q .using switch  check whether vowel or not.

problem 2

soln:
#include <stdio.h>
int main()
{
    // to check whether the letter entered by the user is a vowel or not.
    char ch;
    printf("Enter a letter:\n");
    scanf("%c", &ch);
    switch (ch)
    {
    case 'a':
    case 'e':
    case 'i':
    case 'o':
    case 'u':
    case 'A':
    case 'E':
    case 'I':
    case 'O':
    case 'U':
        printf("%c is a vowel", ch);
        break;
    default:
        printf("%c is not a vowel", ch);
        break;
    }

    return 0;
}
